using System.Numerics;
using NUnit.Framework;
using UnityEngine; 
//using UnityEngine.InputSystem;

//K's Direction Test
namespace Tests 
{ 
    public class DirectionTests
    { 
        [Test] 
        public void Heading() 
        {
            //Create an instance of PlayerController
            //var playerController = new PlayerController();

            // Simulate a movement input
            //playerController.OnMove(InputValue.CreateVector2(new Vector2(0, 1)));

            // Access the movement input and assert against it
            //Assert.AreEqual(expected: new Vector2(0, 1), actual: playerController.movementInput); 
        }
    }
}
